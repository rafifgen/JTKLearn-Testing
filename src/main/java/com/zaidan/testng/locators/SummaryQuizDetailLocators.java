package com.zaidan.testng.locators;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class SummaryQuizDetailLocators {
    @FindBy(id = "sort")
    public WebElement sortDropdown;
}
