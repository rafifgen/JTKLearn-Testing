package com.zaidan.testng.enums;

public enum TaskStatus {
    PASSED,      // Finished with a passing grade (check-circle)
    IN_PROGRESS, // Started but not finished (hourglass)
    NOT_TAKEN    // Not started or failed (x-circle)
}
