spring.application.name=jtklearn-selenium-cucumber

# Database Connection (UNCHANGED, but now connects to local end of tunnel)
db.url=jdbc:postgresql://localhost:5433/jtk_learn_db_dev
db.username=jtk_learn
db.password=P@ssw0rd123

# SSH Tunnel Configuration
ssh.host=cloudias79.com
ssh.port=2323
ssh.username=guest
ssh.password=xE|89]{7h9R?

# Port Forwarding Details
# Local port that your application will connect to (DBeaver uses 5433 locally)
ssh.local_port=5433
# Remote host where the PostgreSQL server is running (from the SSH server's perspective, usually localhost)
ssh.remote_host=localhost
# Remote port where the PostgreSQL server is listening
ssh.remote_port=5432